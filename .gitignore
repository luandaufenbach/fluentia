# Cada projeto tem o seu .gitignore; aqui ficam so os do sistema operacional e do editor.
.DS_Store
Thumbs.db
.idea/
.vscode/

# Backup dos repositorios separados de antes da juncao. Pode apagar quando quiser.
.git.antigo/

# Segredos da publicacao: senha do banco e chave da API. O exemplo e versionado,
# o arquivo real nunca.
publicacao/.env
